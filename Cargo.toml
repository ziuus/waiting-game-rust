[package]
name = "waiting-game-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4.5"

[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
panic = 'abort'
strip = true
